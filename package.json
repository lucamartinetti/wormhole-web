{
  "name": "wormhole-page",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end encrypted file transfer in the browser.",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
